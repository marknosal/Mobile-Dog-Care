import React from "react";
import RequestCard from './RequestCard';

export default function CompleteRequests({ allRequests }) {
    return (
        <div className="completeRequests">
            completeRequests
        </div>
    )
}