import React from "react";
import RequestCard from './RequestCard';

export default function IncompleteRequests({ allRequests }) {
    return (
        <div className="incompleteRequests">
            incompleteRequests
        </div>
    )
}